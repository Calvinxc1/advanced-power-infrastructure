local_governance:
  load:
    - .governance/local/status.yaml
    - .governance/local/factorio-local-overlay.yaml
  notes:
    - Repo-local status and Factorio mod facts override generic branch assumptions when they differ.
